                    <div class="card mb-4">
                        <div class="card-header mb-4 h3">Tags</div>
                        <div class="card-body">
{{ if .Site.Params.widgets.tags }}
{{ if isset .Site.Taxonomies "tags" }}
{{ if not (eq (len .Site.Taxonomies.tags) 0) }}


      <ul class="list-unstyled">
            {{ $current := . }}
            {{ range $name, $items := .Site.Taxonomies.tags }}
            <li{{ if eq $current.RelPermalink ($name | urlize | lower | printf "/tags/%s/") }} class="d-inline-block active"{{ end }}>
                <a  class="d-print-inline-block  m-2 p-2 active" href="{{ "tags/" | relURL }}{{ $name | urlize | lower }}"><i class="bi bi-tag-fill"></i> {{ $name }}</a>
            </li>
            {{ end }}
        </ul>

{{ end }}
{{ end }}
{{ end }}
</div>
</div>
