#!/bin/sh

choice=$(printf "Check Updates\\nMount drive\\nUnmount drive\\nScreenshot" | dmenu -i -p "Select a script:")

case "$choice" in
    "Check Updates") checkup ;;
    "Mount drive") dmenumount ;;
    "Unmount drive") dmenuumount ;;
    "Screenshot") dmenurecord ;;
esac
