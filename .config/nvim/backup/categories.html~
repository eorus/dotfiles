{{ if .Site.Params.widgets.categories }}
{{ if isset .Site.Taxonomies "categories" }}
{{ if not (eq (len .Site.Taxonomies.categories) 0) }}
<div class="card mb-4 border-0">
      <div class="card-header h3">Categories</div>
          <div class="card-body">

        <ul class="nav nav-pills nav-stacked">
            {{ $current := . }}
            {{ range $name, $items := .Site.Taxonomies.categories }}
            <li{{ if eq $current.RelPermalink ($name | urlize | lower | printf "/categories/%s/") }} class="py-4 nav-item active"{{ end }}>
                <a class="badge bg-primary text-decoration-none m-2 p-1" href="{{ "categories/" | relURL }}{{ $name | urlize | lower }}">{{ $name }} ({{ len $items }})</a>
            </li>
            {{ end }}
        </ul>

          </div>

</div>

</div><!-- End sidebar categories-->

{{ end }}
{{ end }}
{{ end }}
